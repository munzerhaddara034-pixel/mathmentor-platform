# Grade 12 GS topic banks — علوم عامة

Official pack: `GS-All.pdf` (on Munzer’s PC, not in Git).

Do **not** photocopy sessions. Seed original MCQs tagged `generated-in-official-style`, easy → medium → hard.

Proposed paper split (refine after filtering the pack):

| Bank | Arabic | Status |
| --- | --- | --- |
| `complex.json` | الأعداد المركبة | later |
| `space-geometry.json` | هندسة الفضاء | later |
| `probability.json` | الاحتمالات | later |
| `functions.json` | دراسة الدوال | later |

Register a live file in `src/lib/topicBanks.ts` with `"certificate": "GS"`. The practice hub lists this branch automatically.
