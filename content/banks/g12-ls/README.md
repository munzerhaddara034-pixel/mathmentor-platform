# Grade 12 LS — علوم الحياة

Typical official paper: mixed MCQ, space geometry, probability, functions analysis.

| File | Arabic | Status |
| --- | --- | --- |
| `mcq-mixed.json` | أسئلة مختلطة | **seeded** |
| `space-geometry.json` | هندسة الفضاء | **seeded** |
| `probability.json` | الاحتمالات | **seeded** |
| `functions.json` | دراسة الدوال | **seeded** |
| `integration.json` | التكامل (فصل إضافي، ليس خانة النموذج الرباعي) | later |

Limits classroom video stays tied to the first slice of `functions.json` (`/classroom/grade-12-ch1`).

Rebuild remaining LS banks: `python3 scripts/build-g12-banks.py`  
Rebuild Functions extras: `python3 scripts/merge-session-style-functions.py`
