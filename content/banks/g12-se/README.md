# Grade 12 SE topic banks — اجتماع واقتصاد

Official pack: `SE_All Sessions.pdf` (on Munzer’s PC, not in Git).

Do **not** photocopy sessions. Seed original MCQs tagged `generated-in-official-style`, easy → medium → hard.

Proposed paper split (refine after filtering the pack):

| Bank | Arabic | Status |
| --- | --- | --- |
| `statistics.json` | الإحصاء | later |
| `probability.json` | الاحتمالات | later |
| `functions.json` | الدوال والمتتاليات | later |
| `applied.json` | رياضيات تطبيقية ومالية | later |

Register a live file in `src/lib/topicBanks.ts` with `"certificate": "SE"`. The practice hub lists this branch automatically.
