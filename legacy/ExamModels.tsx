import { ArrowLeft, ArrowRight, BookOpen, Clock3, Filter, Layers3, RotateCcw } from "lucide-react";
import { Link } from "wouter";
import { useMemo, useState } from "react";
import { examModels, type ExamModel } from "../../../shared/examModels";

type FilterValue = "all" | string;

export default function ExamModels() {
  const [isArabic, setIsArabic] = useState(false);
  const [gradeFilter, setGradeFilter] = useState<FilterValue>("all");
  const [branchFilter, setBranchFilter] = useState<FilterValue>("all");
  const [difficultyFilter, setDifficultyFilter] = useState<FilterValue>("all");
  const filteredModels = useMemo(() => examModels.filter((model) => (gradeFilter === "all" || model.grade === gradeFilter) && (branchFilter === "all" || model.branch === branchFilter) && (difficultyFilter === "all" || model.difficulty === difficultyFilter)), [gradeFilter, branchFilter, difficultyFilter]);
  const hasFilters = gradeFilter !== "all" || branchFilter !== "all" || difficultyFilter !== "all";
  const resetFilters = () => { setGradeFilter("all"); setBranchFilter("all"); setDifficultyFilter("all"); };
  const labels: Record<ExamModel["branch"], [string, string]> = { "Middle School": ["الشهادة المتوسطة", "Middle School"], "Life Sciences": ["علوم الحياة", "Life Sciences"], "General Sciences": ["العلوم العامة", "General Sciences"], "Sociology and Economics": ["الاقتصاد والاجتماعيات", "Sociology & Economics"], "SAT Math": ["رياضيات SAT", "SAT Math"] };
  const difficultyLabels: Record<ExamModel["difficulty"], [string, string]> = { Foundation: ["تأسيسي", "Foundation"], Certificate: ["شهادة", "Certificate"], Advanced: ["متقدم", "Advanced"] };
  return <main dir={isArabic ? "rtl" : "ltr"} className="min-h-screen bg-[#f7f7f4] px-4 py-8 text-[#15233b] sm:px-6 lg:px-10"><div className="mx-auto max-w-6xl"><div className="mb-8 flex flex-wrap items-center justify-between gap-4"><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-[#5e6a7e]">{isArabic ? <ArrowRight size={16} /> : <ArrowLeft size={16} />}{isArabic ? "العودة إلى الأكاديمية" : "Back to academy"}</Link><button type="button" onClick={() => setIsArabic((value) => !value)} className="rounded-full border border-[#d9dcd4] bg-white px-4 py-2 text-sm font-bold shadow-sm">{isArabic ? "English" : "العربية"}</button></div><header className="mb-8 rounded-[2rem] bg-[#14233e] px-6 py-10 text-white shadow-xl sm:px-10"><div className="flex items-center gap-3 text-[#edbd5f]"><BookOpen size={22} /><span className="text-xs font-bold uppercase tracking-[0.22em]">{isArabic ? "نماذج أصلية" : "ORIGINAL MODEL EXAMS"}</span></div><h1 className="mt-4 max-w-4xl font-serif text-4xl tracking-tight sm:text-6xl">{isArabic ? "نماذج تدريب تحاكي الامتحانات الحقيقية" : "Practice models built for the real exam rhythm."}</h1><p className="mt-4 max-w-3xl text-base leading-7 text-slate-300">{isArabic ? "ابحث حسب الصف والفرع ومستوى الصعوبة للوصول إلى النموذج المناسب." : "Filter by grade, branch, and difficulty to find the right model."}</p></header><section aria-label={isArabic ? "فلاتر النماذج" : "Exam model filters"} className="mb-8 rounded-[1.5rem] border border-[#e3e6df] bg-white p-5 shadow-sm"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-2"><Filter size={18} className="text-[#b07820]" /><h2 className="font-bold text-[#14233e]">{isArabic ? "تصفية النماذج" : "Filter models"}</h2></div><button type="button" onClick={resetFilters} disabled={!hasFilters} className="inline-flex items-center gap-2 rounded-full border border-[#d9dcd4] px-3 py-2 text-xs font-bold text-[#5e6a7e] transition hover:bg-[#f7f9f5] disabled:cursor-not-allowed disabled:opacity-40"><RotateCcw size={14} />{isArabic ? "إعادة الضبط" : "Reset filters"}</button></div><div className="grid gap-3 md:grid-cols-3"><label className="grid gap-1 text-xs font-bold uppercase tracking-wider text-[#738096]">{isArabic ? "الصف الدراسي" : "Grade"}<select value={gradeFilter} onChange={(event) => setGradeFilter(event.target.value)} className="h-11 rounded-xl border border-[#d9dcd4] bg-[#f7f9f5] px-3 text-sm font-semibold text-[#14233e] outline-none focus:border-[#b07820]"><option value="all">{isArabic ? "كل الصفوف" : "All grades"}</option><option value="Grade 9">{isArabic ? "الصف التاسع" : "Grade 9"}</option><option value="Grade 12">{isArabic ? "الصف الثاني عشر" : "Grade 12"}</option><option value="SAT">SAT</option></select></label><label className="grid gap-1 text-xs font-bold uppercase tracking-wider text-[#738096]">{isArabic ? "الفرع" : "Branch"}<select value={branchFilter} onChange={(event) => setBranchFilter(event.target.value)} className="h-11 rounded-xl border border-[#d9dcd4] bg-[#f7f9f5] px-3 text-sm font-semibold text-[#14233e] outline-none focus:border-[#b07820]"><option value="all">{isArabic ? "كل الفروع" : "All branches"}</option>{Object.entries(labels).map(([value, label]) => <option key={value} value={value}>{isArabic ? label[0] : label[1]}</option>)}</select></label><label className="grid gap-1 text-xs font-bold uppercase tracking-wider text-[#738096]">{isArabic ? "مستوى الصعوبة" : "Difficulty"}<select value={difficultyFilter} onChange={(event) => setDifficultyFilter(event.target.value)} className="h-11 rounded-xl border border-[#d9dcd4] bg-[#f7f9f5] px-3 text-sm font-semibold text-[#14233e] outline-none focus:border-[#b07820]"><option value="all">{isArabic ? "كل المستويات" : "All levels"}</option>{Object.entries(difficultyLabels).map(([value, label]) => <option key={value} value={value}>{isArabic ? label[0] : label[1]}</option>)}</select></label></div><p className="mt-4 text-sm text-[#657184]">{isArabic ? `${filteredModels.length} نماذج متاحة` : `${filteredModels.length} model${filteredModels.length === 1 ? "" : "s"} available`}</p></section>{filteredModels.length === 0 ? <div className="rounded-[1.5rem] border border-dashed border-[#cfd5cb] bg-white px-6 py-14 text-center"><p className="font-serif text-3xl text-[#14233e]">{isArabic ? "لا توجد نماذج مطابقة" : "No matching models"}</p><p className="mt-2 text-sm text-[#657184]">{isArabic ? "جرّب تغيير الفلاتر أو إعادة ضبطها." : "Try changing the filters or reset them to see all models."}</p><button type="button" onClick={resetFilters} className="mt-5 rounded-full bg-[#edbd5f] px-5 py-3 text-sm font-bold text-[#14233e]">{isArabic ? "عرض كل النماذج" : "Show all models"}</button></div> : <div className="grid gap-5 lg:grid-cols-2">{filteredModels.map((model) => <article key={model.id} className="rounded-[1.75rem] border border-[#e3e6df] bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#b07820]">{model.track}</p><h2 className="mt-2 font-serif text-3xl text-[#14233e]">{isArabic ? model.arabicTitle : model.title}</h2></div><div className="flex flex-wrap gap-2"><span className="rounded-full bg-[#fff5df] px-3 py-1 text-sm font-bold text-[#8a611b]">{model.questions.length} {isArabic ? "أسئلة" : "questions"}</span><span className="rounded-full bg-[#edf4ee] px-3 py-1 text-xs font-bold text-[#3d6b4c]">{isArabic ? difficultyLabels[model.difficulty][0] : difficultyLabels[model.difficulty][1]}</span></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-[#f7f9f5] p-4"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-[#738096]"><Clock3 size={15} />{isArabic ? "المدة" : "Duration"}</div><p className="mt-2 text-lg font-bold text-[#14233e]">{model.durationMinutes} {isArabic ? "دقيقة" : "minutes"}</p></div><div className="rounded-2xl bg-[#f7f9f5] p-4"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-[#738096]"><Layers3 size={15} />{isArabic ? "الأقسام" : "Sections"}</div><p className="mt-2 text-lg font-bold text-[#14233e]">{model.sections.length}</p></div></div><div className="mt-5 space-y-2">{model.sections.map((section) => <div key={section.title} className="flex items-center justify-between gap-3 rounded-xl border border-[#edf0eb] px-4 py-3 text-sm"><span className="font-semibold">{isArabic ? section.arabicTitle : section.title}</span><span className="text-[#738096]">{section.questionCount} {isArabic ? "أسئلة" : "questions"}</span></div>)}</div><p className="mt-5 text-sm leading-6 text-[#657184]">{model.sourceNote}</p><Link href={model.startPath} className="mt-6 inline-flex items-center gap-2 rounded-full bg-[#edbd5f] px-5 py-3 text-sm font-bold text-[#14233e] transition hover:bg-[#f4ca78]">{isArabic ? "ابدأ التدريب" : "Start practice"}{isArabic ? <ArrowLeft size={16} /> : <ArrowRight size={16} />}</Link></article>)}</div>}</div></main>;
}
